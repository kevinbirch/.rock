
(require :asdf)

(pushnew "/Users/kmb/.lisp/" asdf:*central-registry* :test #'equal)

(require :asdf-install)

#-quicklisp
(let ((quicklisp-init (merge-pathnames ".lisp/quicklisp/setup.lisp" (user-homedir-pathname))))
  (when (probe-file quicklisp-init)
    (load quicklisp-init)))


